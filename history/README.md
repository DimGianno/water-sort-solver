# History

This folder contains older snapshots of the project to show progress over time.

- `v0-singlefile/` — initial single HTML version (BTS algorithm)
- `v1-singlefile/` — initial single HTML version (A* algorithm)

